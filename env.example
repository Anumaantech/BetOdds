# Environment Configuration for Render Deployment
# Copy this file and set these variables in your Render service settings

# General Configuration
NODE_ENV=production

# Port Configuration 
# NOTE: Render automatically sets PORT environment variable for each service
# For local development, uncomment and set specific ports:
# PORT=3000  # For monitoring service when running locally
# PORT=3005  # For cricket API when running locally

# Application Configuration
ENABLE_NOTIFICATIONS=false
PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome-stable
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true

# Database Configuration
# For Render deployment: This will be automatically injected via fromDatabase
# For local development: Replace with your actual MongoDB connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/cricket_data?retryWrites=true&w=majority
MONGODB_DB_NAME=cricket_data

# Optional: API Keys (if using external services)
# GROQ_API_KEY=your_groq_api_key_here
# OPENAI_API_KEY=your_openai_api_key_here

# Optional: Additional Configuration
# MAX_CONCURRENT_URLS=10
# BATCH_PROCESSING_INTERVAL=1000 